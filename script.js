const scene=document.querySelector('.scene'),button=document.querySelector('.big-star'),canvas=document.querySelector('.message-stars'),tiny=document.querySelector('.tiny-stars');for(let i=0;i<35;i++){const e=document.createElement('i');e.style.setProperty('--i',i);e.textContent='✦';tiny.append(e)}
const melody=[[392,.28],[392,.18],[440,.45],[392,.45],[523,.45],[494,.8],[392,.28],[392,.18],[440,.45],[392,.45],[587,.45],[523,.8],[392,.28],[392,.18],[784,.45],[659,.45],[523,.45],[494,.45],[440,.8],[698,.28],[698,.18],[659,.45],[523,.45],[587,.45],[523,.9]];
function song(){const C=window.AudioContext||window.webkitAudioContext,c=new C();let t=c.currentTime+.05;melody.forEach(([f,d])=>{const o=c.createOscillator(),g=c.createGain();o.type='triangle';o.frequency.value=f;g.gain.setValueAtTime(.001,t);g.gain.linearRampToValueAtTime(.09,t+.025);g.gain.setValueAtTime(.07,t+d*.72);g.gain.exponentialRampToValueAtTime(.001,t+d);o.connect(g).connect(c.destination);o.start(t);o.stop(t+d+.02);t+=d})}
function draw(){const d=Math.min(devicePixelRatio,2),w=innerWidth,h=innerHeight,c=canvas.getContext('2d');canvas.width=w*d;canvas.height=h*d;canvas.style.width=w+'px';canvas.style.height=h+'px';c.scale(d,d);const size=Math.min(66,w*.155);c.font='900 '+size+'px Arial';c.textAlign='center';c.textBaseline='middle';c.fillStyle='#fff';c.fillText('HAPPY',w/2,h*.35);c.fillText('BIRTHDAY',w/2,h*.49);const data=c.getImageData(0,0,w*d,h*d).data;c.clearRect(0,0,w,h);const gap=Math.max(8,Math.round(w/48));let n=0;for(let y=h*.24|0;y<h*.59;y+=gap)for(let x=gap;x<w-gap;x+=gap){const p=((Math.floor(y*d)*w*d+Math.floor(x*d))*4)+3;if(data[p]>100){n++;const r=1.4+(n%3)*.45;c.beginPath();for(let k=0;k<10;k++){const a=-Math.PI/2+k*Math.PI/5,rr=k%2?r*.42:r;c.lineTo(x+Math.cos(a)*rr,y+Math.sin(a)*rr)}c.closePath();c.fillStyle=`hsl(${42+(n%5)*7} 95% ${68+(n%3)*8}%)`;c.shadowColor='#ffe89a';c.shadowBlur=7;c.fill()}}}
button.addEventListener('click',()=>{if(scene.classList.contains('on'))return;scene.classList.add('on');song();setTimeout(draw,400)});addEventListener('resize',()=>scene.classList.contains('on')&&draw());
